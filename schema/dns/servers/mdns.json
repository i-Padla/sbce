{
    "type": "object",
    "title": "mDNS",
    "x-propertiesToggleContent": "Toggle properties",
    "x-format": "grid",
    "properties": {
        "type": {
            "type": "string",
            "default": "mdns",
            "const": "mdns",
            "readOnly": true,
            "x-grid": {
                "columns": 6
            }
        },
        "tag": {
            "type": "string",
            "default": "mdns-dns",
            "x-grid": {
                "columns": 6
            }
        },
        "interface": {
            "type": "array",
            "description": "List of network interface names to send mDNS queries on.\n When empty, all interfaces that are up, multicast-capable, and non-loopback are used.",
            "x-format": "table",
            "x-arrayDeleteContent": "",
            "x-arrayAddContent": "Add",
            "x-arrayMove": false,
            "x-enableCollapseToggle": true,
            "x-collapseToggleContent": "",
            "items": {
                "type": "string"
            }
        }
    },
    "required": [
        "type",
        "tag"
    ]
}