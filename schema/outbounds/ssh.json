{
    "type": "object",
    "title": "{{ value.tag}} ({{ value.type }})",
    "x-format": "grid",
    "properties": {
        "type": {
            "type": "string",
            "default": "ssh",
            "const": "ssh",
            "readOnly": true,
            "x-grid": {
                "columns": 6
            }
        },
        "tag": {
            "type": "string",
            "default": "ssh-out",
            "x-grid": {
                "columns": 6
            }
        }
    },
    "required": [
        "type",
        "tag"
    ]
}