{
    "type": "object",
    "title": "{{ value.tag}} ({{ value.type }})",
    "x-format": "grid",
    "properties": {
        "type": {
            "type": "string",
            "default": "vmess",
            "const": "vmess",
            "readOnly": true,
            "x-grid": {
                "columns": 6
            }
        },
        "tag": {
            "type": "string",
            "default": "vmess-out",
            "x-grid": {
                "columns": 6
            }
        }
    },
    "required": [
        "type",
        "tag"
    ]
}