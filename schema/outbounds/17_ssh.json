{
    "type": "object",
    "title": "SSH",
    "x-format": "grid",
    "required": [
        "type",
        "tag"
    ],
    "properties": {
        "type": {
            "type": "string",
            "default": "ssh",
            "const": "ssh",
            "readOnly": true,
            "x-grid": {
                "columns": 6
            }
        },
        "tag": {
            "type": "string",
            "default": "ssh-out",
            "x-grid": {
                "columns": 6
            }
        }
    }
}